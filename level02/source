#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
    char    username[100];
    char    pass_file[48];
    char    password[112];
    int     bytes_read;
    FILE    *fp;

    memset(username,  0, sizeof(username));
    memset(pass_file, 0, sizeof(pass_file));
    memset(password,  0, sizeof(password));

    fp = fopen("/home/users/level03/.pass", "r");
    if (fp == NULL) {
        fwrite("ERROR: failed to open password file\n", 1, 36, stderr);
        exit(1);
    }

    bytes_read = (int)fread(pass_file, 1, 41, fp);
    pass_file[strcspn(pass_file, "\n")] = '\0';

    if (bytes_read != 41) {
        fwrite("ERROR: failed to read password file\n", 1, 36, stderr);
        fwrite("ERROR: failed to read password file\n", 1, 36, stderr);
        exit(1);
    }
    fclose(fp);

    puts("===== [ Secure Access System v1.0 ] =====");
    puts("/***************************************\\");
    puts("| You must login to access this system. |");
    puts("\\**************************************/");

    printf("--[ Username: ");
    fgets(username, 100, stdin);
    username[strcspn(username, "\n")] = '\0';

    printf("--[ Password: ");
    fgets(password, 100, stdin);
    password[strcspn(password, "\n")] = '\0';

    puts("*****************************************");

    if (strncmp(pass_file, password, 41) == 0) {
        printf("Greetings, %s!\n", username);
        system("/bin/sh");
        return 0;
    }

    printf(username); // format string vulnerability : username utilisé directement comme format
    puts(" does not have access!");
    exit(1);
}
